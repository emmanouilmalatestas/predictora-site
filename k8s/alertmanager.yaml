route:
  receiver: "telegram-notifications"
  group_by: ["alertname"]
  group_wait: 10s
  group_interval: 1m
  repeat_interval: 10m

receivers:
  - name: "telegram-notifications"
    webhook_configs:
      - url: "http://telegram-alert-proxy:8080/alert"
        send_resolved: true
