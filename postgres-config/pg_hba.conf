# TYPE  DATABASE        USER            ADDRESS                 METHOD

# 1️⃣ Allow local connections (Unix socket)
local   all             all                                     md5

# 2️⃣ Allow internal Docker network only (backend_internal)
host    all             all             172.20.0.0/16           md5

# 3️⃣ Deny ALL external IPv4
host    all             all             0.0.0.0/0               reject

# 4️⃣ Deny ALL external IPv6
host    all             all             ::/0                    reject
