apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: predictora-cert
  namespace: predictora
spec:
  secretName: predictora-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  commonName: api.predictoraai.com
  dnsNames:
    - api.predictoraai.com
